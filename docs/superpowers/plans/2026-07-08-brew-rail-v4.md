# 진행 레일 v4 "스텝 필" Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Brewing 진행 레일에서 물방울·다리·흡수 장식을 전부 제거하고, 붓기별 분리 pill 구간(8px, 250ms 채움 보간)만 남긴다.

**Architecture:** `BrewRail.tsx`의 세그먼트 렌더는 유지하고 경계 anchor(물방울) 블록만 삭제. 기하·색은 전부 `brewing.css` 토큰이므로 토큰 값 변경으로 두께·틈·색을 조정. 스펙: `docs/superpowers/specs/2026-07-08-brew-rail-v4-design.md`.

**Tech Stack:** React 19 + TypeScript strict, Vitest 2 + @testing-library/react (jsdom), Tailwind 3 + CSS variables.

## Global Constraints

- 색/크기 하드코딩 금지 — 모든 스타일 값은 `brewing.css` 토큰의 CSS 변수 참조 (CLAUDE.md 원칙 4).
- 도메인(`packages/domain`) 변경 없음.
- BrewingScreen 레이아웃·정보 구조 변경 없음 — 레일 내부 교체만.
- 리드인 시각 신호를 레일에 넣지 않음 (배경 물듦 + 소리 큐가 담당).
- 작업 브랜치: `feat/brew-rail-v4-step-fill` (main에서 분기).

---

### Task 1: BrewRail 컴포넌트 단순화 (테스트 → 구현)

**Files:**
- Modify: `apps/web/src/features/brewing/BrewRail.test.tsx`
- Modify: `apps/web/src/features/brewing/BrewingScreen.test.tsx:132-138` (진행 레일 describe)
- Modify: `apps/web/src/features/brewing/BrewRail.tsx`
- Modify: `apps/web/src/features/brewing/BrewRail.css`
- Modify: `apps/web/src/ui/tokens/components/brewing.css`

**Interfaces:**
- Consumes: `BrewRail({ pours, totalTimeSec, elapsed })` — props 시그니처 불변.
- Produces: `data-testid="brew-rail"`(컨테이너) / `"brew-rail-fill"`(구간별 채움)만 존재. `"brew-rail-drop"`은 더 이상 렌더되지 않음 (Task 1 이후 어디에서도 참조 금지).

- [ ] **Step 1: 브랜치 생성**

```bash
git checkout -b feat/brew-rail-v4-step-fill
```

- [ ] **Step 2: BrewRail.test.tsx 갱신 — 물방울 describe 삭제, 부재 단언 추가**

파일 전체를 아래로 교체:

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import type { Pour } from "@pourover/domain/types";
import { g, s } from "@pourover/domain/units";
import { BrewRail } from "./BrewRail";

const mkPour = (i: number, atSec: number, amt: number, cum: number): Pour => ({
  index: i,
  atSec: s(atSec),
  pourAmount: g(amt),
  cumulativeWater: g(cum),
});

// 45s·75s 경계, 총 210s — BrewingScreen 테스트와 같은 레시피 골격
const pours = [
  mkPour(0, 0, 30, 30),
  mkPour(1, 45, 120, 150),
  mkPour(2, 75, 100, 250),
];

const renderRail = (elapsed: number) =>
  render(<BrewRail pours={pours} totalTimeSec={210} elapsed={elapsed} />);

describe("BrewRail — 세그먼트", () => {
  it("푸어 개수만큼 세그먼트, 경과에 따라 구간별 채움", () => {
    renderRail(60);
    const fills = screen.getAllByTestId("brew-rail-fill");
    expect(fills).toHaveLength(3);
    // CSSOM이 "100.00%" → "100%"로 소수점 0을 정규화한다
    expect(fills[0]!.style.width).toBe("100%"); // 0–45 완료
    expect(fills[1]!.style.width).toBe("50%"); // (60-45)/(75-45)
    expect(fills[2]!.style.width).toBe("0%"); // 미도달
  });

  it("경계 마커(물방울)를 렌더하지 않는다 — 구간 사이 틈이 곧 경계", () => {
    renderRail(60);
    expect(screen.queryByTestId("brew-rail-drop")).toBeNull();
  });
});
```

- [ ] **Step 3: BrewingScreen.test.tsx의 물방울 단언 교체**

"진행 레일" describe(132행 부근)에서:

```tsx
    expect(screen.getByTestId("brew-rail")).toBeInTheDocument();
    expect(screen.getAllByTestId("brew-rail-drop")).toHaveLength(2); // 45s, 75s
```

를 다음으로 교체:

```tsx
    expect(screen.getByTestId("brew-rail")).toBeInTheDocument();
    expect(screen.getAllByTestId("brew-rail-fill")).toHaveLength(3); // 푸어 3회
```

- [ ] **Step 4: 테스트 실행 — 실패 확인**

Run: `cd apps/web && bunx vitest run src/features/brewing/BrewRail.test.tsx src/features/brewing/BrewingScreen.test.tsx`
Expected: FAIL — "경계 마커(물방울)를 렌더하지 않는다"에서 `brew-rail-drop`이 아직 렌더되므로 실패. (세그먼트 테스트는 그린.)

- [ ] **Step 5: BrewRail.tsx 단순화**

파일 전체를 아래로 교체 (`DROP_PATH`, `LEAD_IN_SEC`, 경계 anchor 블록 삭제):

```tsx
import type { Pour } from "@pourover/domain/types";
import { toPct } from "@/ui/format";
import "./BrewRail.css";

const GAP_HALF = "var(--size-brewing-rail-gap) / 2";

const clamp01 = (v: number): number => Math.min(1, Math.max(0, v));

type Props = {
  readonly pours: readonly Pour[];
  readonly totalTimeSec: number;
  readonly elapsed: number;
};

// 진행 레일 "스텝 필" — 붓기마다 분리된 pill 구간, 구간 사이 틈이 곧 붓기 경계
// (별도 마커 없음). 채움 폭은 useElapsed 250ms 틱 사이를 CSS transition이 보간.
// 정보는 구석 텍스트·aria-live가 담당하므로 레일 자체는 장식(aria-hidden).
export function BrewRail({ pours, totalTimeSec, elapsed }: Props) {
  if (totalTimeSec <= 0) return null;

  const segments = pours.map((pour, i) => ({
    startSec: pour.atSec,
    endSec: pours[i + 1]?.atSec ?? totalTimeSec,
  }));

  return (
    <div data-testid="brew-rail" aria-hidden="true" className="brew-rail mx-5">
      {segments.map((seg) => (
        <div
          key={seg.startSec}
          className="brew-rail-seg"
          style={{
            left:
              seg.startSec === 0
                ? "0"
                : `calc(${toPct(seg.startSec / totalTimeSec)} + ${GAP_HALF})`,
            right:
              seg.endSec >= totalTimeSec
                ? "0"
                : `calc(${toPct(1 - seg.endSec / totalTimeSec)} + ${GAP_HALF})`,
          }}
        >
          <div
            data-testid="brew-rail-fill"
            className="brew-rail-seg-fill"
            style={{
              width: toPct(
                clamp01((elapsed - seg.startSec) / (seg.endSec - seg.startSec)),
              ),
            }}
          />
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 6: BrewRail.css 단순화**

파일 전체를 아래로 교체 (boundary/bridge/drop/sink/quiver 삭제, fill에 보간 추가):

```css
/* BrewRail — 진행 레일 "스텝 필". 색·기하는 전부 brewing.css의 rail 토큰 참조.
   붓기마다 분리된 pill 구간 — 구간 사이 틈이 곧 붓기 경계라 별도 마커가 없다. */

.brew-rail {
  position: relative;
  height: var(--size-brewing-rail-h);
}

.brew-rail-seg {
  position: absolute;
  top: 0;
  bottom: 0;
  border-radius: var(--radius-pill);
  background: var(--color-brewing-rail-track);
  overflow: hidden;
}

.brew-rail-seg-fill {
  position: absolute;
  inset: 0 auto 0 0;
  border-radius: var(--radius-pill);
  background: var(--color-brewing-rail-fill);
  transition: width 250ms linear; /* useElapsed 250ms 틱 사이 채움 보간 */
}

@media (prefers-reduced-motion: reduce) {
  .brew-rail-seg-fill {
    transition: none;
  }
}
```

- [ ] **Step 7: brewing.css 토큰 정리**

rail 관련 블록(라이트 15–26행, 다크 38–40행)을 아래로 교체. base/leadin 토큰은 그대로 둔다:

라이트(`:root`):

```css
  /* 진행 레일 "스텝 필" — 붓기마다 분리된 pill 구간, 구간 사이 틈이 곧 붓기 경계. */
  --color-brewing-rail-track: var(--neutral-100);
  --color-brewing-rail-fill: var(--accent-500);

  /* 레일 기하 — 라이트/다크 공통 */
  --size-brewing-rail-h: 8px; /* pill 구간 두께 */
  --size-brewing-rail-gap: 6px; /* 붓기 경계 틈 */
```

다크(`[data-theme="dark"]`):

```css
  --color-brewing-rail-track: var(--neutral-700);
  --color-brewing-rail-fill: var(--accent-400);
```

삭제되는 토큰: `--color-brewing-rail-drop`(라이트·다크), `--size-brewing-rail-line`, `--size-brewing-rail-drop-w`, `--size-brewing-rail-air`.

- [ ] **Step 8: 테스트 실행 — 통과 확인**

Run: `cd apps/web && bunx vitest run src/features/brewing/`
Expected: PASS (BrewRail 2건 + BrewingScreen 전체 그린)

- [ ] **Step 9: 잔여 참조 확인**

Run: `grep -rn "rail-drop\|rail-air\|rail-line\|brew-rail-boundary\|brew-rail-bridge" apps/web/src`
Expected: 출력 없음 (남은 참조 0)

- [ ] **Step 10: 커밋**

```bash
git add apps/web/src/features/brewing/BrewRail.tsx apps/web/src/features/brewing/BrewRail.css apps/web/src/features/brewing/BrewRail.test.tsx apps/web/src/features/brewing/BrewingScreen.test.tsx apps/web/src/ui/tokens/components/brewing.css
git commit -m "feat(brewing): 진행 레일 v4 \"스텝 필\" — 물방울 장식 제거, 분리 pill 구간으로 단순화"
```

---

### Task 2: 메타 문서 갱신 + 전체 검증

**Files:**
- Modify: `docs/design-tokens.md:262-265` (Brewing 토큰 섹션 — `rail-{track,fill,tick}` 표기가 이미 낡음)

**Interfaces:**
- Consumes: Task 1의 최종 토큰 세트 (`rail-{track,fill}` 색 + `rail-{h,gap}` 크기).
- Produces: 없음 (문서·검증만).

- [ ] **Step 1: design-tokens.md Brewing 섹션 갱신**

```css
/* 진행 레일 — 트랙 위 경과 채움 + 푸어 경계 눈금 */
--color-brewing-rail-{track,fill,tick}: …;
```

를 다음으로 교체:

```css
/* 진행 레일 "스텝 필" — 붓기마다 분리된 pill 구간, 구간 사이 틈이 곧 경계 */
--color-brewing-rail-{track,fill}: …;
```

같은 섹션 454행 부근 표의 `brewing-rail-{track,fill,tick}` 표기도 `brewing-rail-{track,fill}`로 맞춘다.

- [ ] **Step 2: 전체 검증 (typecheck + lint + 전체 테스트)**

Run: `bun run typecheck && bun run lint && bun run test:run`
Expected: 모두 PASS — lint는 토큰 규약 위반(하드코딩 색/크기)을 자동 차단하므로 여기서 그린이면 규약 준수 확인 완료.

- [ ] **Step 3: 육안 확인 (라이트/다크)**

Run: `bun run dev` 후 브루잉 화면 진입, 라이트·다크 각각에서 확인:
- 구간이 8px pill로 렌더되고 틈 6px이 경계마다 보인다
- 채움이 끊김 없이 진행된다 (250ms 보간)
- 물방울·마커 잔재 없음

기준 목업: `.superpowers/brainstorm/19722-1783468266/content/rail-hybrid-v2.html`

- [ ] **Step 4: 커밋**

```bash
git add docs/design-tokens.md
git commit -m "docs(tokens): 진행 레일 v4 토큰 표기 갱신 (drop/tick 제거)"
```
